spring:
  application:
    name: gopang-eureka
  profiles:
    active: default
  cloud:
    config:
      uri: http://configserver:8071
      name: gopang-eureka
    loadbalancer:
      ribbon:
        enabled: false