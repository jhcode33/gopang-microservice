spring:
  application:
    name: gopang-eureka
  profiles:
    active: local
  cloud:
    config:
      uri: http://localhost:8071
      name: gopang-eureka
    loadbalancer:
      ribbon:
        enabled: false